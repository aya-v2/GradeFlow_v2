{% extends "base.html.twig" %}

{% block title %}Système & Archives{% endblock %}

{% block body %}
    {% include 'components/sidebar.html.twig' %}

    <main class="min-h-screen p-8 bg-white">
        <div class="max-w-7xl mx-auto">
            <header class="mb-8">
                <h1 class="text-4xl font-bold text-gray-900">Système & Archives</h1>
                <p class="text-gray-500 text-sm mt-2">Gérez les années scolaires, les archives et les actions système</p>
            </header>

            <!-- School Year Management -->
            <div class="bg-white rounded-2xl shadow-sm border border-slate-100 p-6 mb-8">
                <h2 class="text-2xl font-bold text-gray-900 mb-6">Années Scolaires</h2>
                
                {% if currentYear %}
                    <div class="mb-6 bg-green-50 border border-green-200 rounded-2xl px-6 py-4">
                        <p class="text-green-800 font-medium">Année Actuelle: <span class="font-bold text-lg">{{ currentYear.name }}</span></p>
                    </div>
                {% endif %}

                <div class="space-y-3 mb-6">
                    {% for year in schoolYears %}
                    <div class="flex items-center justify-between p-4 border border-slate-200 rounded-lg hover:bg-slate-50 transition">
                        <div>
                            <p class="font-semibold text-gray-900">{{ year.name }}</p>
                            <p class="text-sm text-gray-500 mt-1">
                                {% if year.isCurrent %}
                                    <span class="inline-block px-2 py-1 bg-green-100 text-green-800 rounded-full text-xs font-bold">Actuelle</span>
                                {% else %}
                                    <span class="inline-block px-2 py-1 bg-gray-100 text-gray-800 rounded-full text-xs font-bold">Archivée</span>
                                {% endif %}
                            </p>
                        </div>
                        <div class="text-right text-sm text-gray-600">
                            Créée le: {{ year.id }}
                        </div>
                    </div>
                    {% endfor %}
                </div>

                <!-- Start New Year Action -->
                <div class="bg-amber-50 border border-amber-200 rounded-2xl px-6 py-6">
                    <h3 class="font-bold text-amber-900 mb-2">⚠ Démarrer une Nouvelle Année</h3>
                    <p class="text-amber-800 text-sm mb-4">
                        Cliquez sur le bouton ci-dessous pour démarrer une nouvelle année scolaire. 
                        Cela marquera l'année actuelle comme archivée et créera une nouvelle année.
                    </p>
                    <form method="post" action="{{ path('app_admin_new_year') }}" onsubmit="return confirm('Êtes-vous sûr ? Cette action va créer une nouvelle année scolaire et archiver la courante.');">
                        <button type="submit" class="px-6 py-3 bg-amber-600 text-white rounded-lg hover:bg-amber-700 transition font-bold">
                            Démarrer Nouvelle Année (2026-2027)
                        </button>
                    </form>
                </div>
            </div>

            <!-- Archives Section -->
            <div class="bg-white rounded-2xl shadow-sm border border-slate-100 p-6">
                <h2 class="text-2xl font-bold text-gray-900 mb-6">Archives</h2>
                
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <p class="text-sm text-gray-600 mb-4">
                            Accédez aux archives scolaires des années précédentes.
                        </p>
                        <button class="w-full px-4 py-2 border border-indigo-600 text-indigo-600 rounded-lg hover:bg-indigo-50 transition font-medium disabled opacity-50 cursor-not-allowed">
                            Bientôt disponible
                        </button>
                </div>
            </div>
        </div>
    </main>

    {% include 'components/sidebar_close.html.twig' %}
{% endblock %}
